# 前端面试题 - 工程化与性能优化

## 1. HMR (Hot Module Replacement) 热更新是什么？

### 核心概念
**HMR** 全称 **Hot Module Replacement**（模块热替换），是现代前端构建工具（如 Webpack、Vite）提供的一项功能。

### 主要作用
在应用程序运行过程中，**替换、添加或删除模块，而无需重新加载整个页面**。

### 解决了什么问题？
1.  **保留应用状态**：这是 HMR 与“自动刷新（Live Reload）”最大的区别。普通刷新会丢失当前页面的所有状态（如弹窗打开状态、表单输入内容），而 HMR 可以保留这些状态，只更新变更的代码逻辑。
2.  **提升开发效率**：只更新变更的模块，速度极快（通常 <200ms），提供近乎即时的反馈循环。
3.  **样式调试便捷**：修改 CSS 时，HMR 可以直接替换样式文件，无需刷新页面，调整 UI 非常方便。

### 在本项目中的应用
在简历中提到的 *"实施 Vite 7.1 构建工具链，HMR 热更新 <200ms"*，体现了项目使用了高性能的构建工具 Vite。
- **Vite 的 HMR 优势**：相比 Webpack 需要重新编译打包，Vite 利用浏览器原生 ESM 能力，HMR 仅需让浏览器重新请求变更的模块，因此速度极快，且不随项目体积增大而显著变慢。

### 简述原理（加分项）
1.  **监听文件变化**：构建工具监听文件系统。
2.  **构建更新模块**：当文件变化时，重新编译该模块。
3.  **推送更新消息**：通过 WebSocket 向浏览器发送更新消息（包含更新模块的 hash 或路径）。
4.  **浏览器接收并替换**：浏览器运行时（HMR Runtime）接收消息，通过 JSONP 或动态 import 拉取新模块代码。
5.  **冒泡更新**：如果当前模块无法自我接受更新（Self Accept），则向上冒泡，直到有父模块接受更新，或者刷新页面。

## 2. manualChunks 代码分割策略是什么？

### 核心概念
**manualChunks** 是 Vite（底层基于 Rollup）构建配置中的一个选项，用于**自定义代码分割（Code Splitting）策略**。

### 主要作用
默认情况下，构建工具会将所有代码打包成一个或几个巨大的文件（bundle）。`manualChunks` 允许开发者**手动指定哪些模块应该被打包到同一个独立的文件（chunk）中**。

### 解决了什么问题？
1.  **优化缓存（Cache Optimization）**：将第三方库（如 React, Mapbox, Recharts）打包成独立的 chunk。因为这些库的代码很少变动，用户浏览器可以**长期缓存**这些文件。当你只修改业务代码时，用户只需要重新下载很小的业务代码包，而不需要重新下载巨大的第三方库。
2.  **减少首屏体积**：避免将所有代码都加载到首屏。
3.  **避免重复打包**：确保多个入口文件引用的公共模块只被打包一次。

### 在本项目中的应用
在简历中提到的 *"配置 manualChunks 代码分割策略将 vendor 库（react、mapbox-gl、recharts、utils）独立打包"*，意味着通过配置 `vite.config.ts`，强制将这些体积较大的第三方库从主包中剥离出来。

**效果对比：**
*   **构建前**：可能是一个巨大的 `index.js` (2MB+)，用户每次打开都要加载这 2MB。
*   **构建后**：变成了 `index.js` (50KB, 业务代码) + `vendor-react.js` (100KB) + `vendor-map.js` (500KB)。
*   **收益**：用户第二次访问时，`vendor-*.js` 直接从缓存读取，只需下载 50KB 的新业务代码，页面加载速度极大提升。简历中提到的 **"首屏 bundle 减少 89%"** 正是得益于此。

## 3. React.lazy() + Suspense 懒加载是什么？

### 核心概念
**React.lazy()** 和 **Suspense** 是 React 原生提供的**代码分割（Code Splitting）**方案，用于实现组件的**按需加载（Lazy Loading）**。

*   **React.lazy()**：允许你像渲染常规组件一样处理动态引入（Dynamic Import）的组件。它接受一个函数，该函数需要动态调用 `import()`。
*   **Suspense**：是一个包裹组件，允许你在组件树中等待某些操作（如懒加载组件代码）完成，并在等待期间显示一个**后备 UI（Fallback UI）**，例如加载指示器（Spinner）。

### 解决了什么问题？
1.  **减小首屏体积（核心价值）**：现代单页应用（SPA）通常将所有页面打包成一个巨大的 JS 文件。用户访问首页时，不得不下载所有页面的代码（包括用户可能永远不会访问的“设置页”或“复杂报表页”）。懒加载可以将这些非首屏组件拆分成独立的文件，**只有当用户真正导航到该页面时，才去网络请求加载对应的代码**。
2.  **提升首屏加载速度（FCP/LCP）**：由于主包体积变小，浏览器解析和执行 JS 的时间大幅缩短。

### 在本项目中的应用
在简历中提到的 *"使用 React.lazy() + Suspense 对 AnalyticsPage 等3个大型组件实施懒加载"*。

**场景分析**：
*   `AnalyticsPage` 包含大量的图表库（Recharts）和统计算法，代码体积很大。
*   用户首次进入系统主要看地图（MapView），不一定马上看分析报表。
*   **优化前**：用户打开地图，浏览器被迫下载了包含 AnalyticsPage 的巨大 bundle。
*   **优化后**：用户打开地图，只下载地图相关代码。当用户点击“分析”按钮时，浏览器才开始下载 `AnalyticsPage.js`，期间展示 Loading 动画。

**代码示例**：
```tsx
// 1. 动态引入组件（不使用静态 import）
const AnalyticsPage = React.lazy(() => import('./components/AnalyticsPage'));

// 2. 在路由或组件中使用 Suspense 包裹
function App() {
  return (
    <div>
      <Header />
      <Suspense fallback={<div className="loading">Loading Analytics...</div>}>
        {/* 只有当 showAnalytics 为 true 时，才会去网络请求 AnalyticsPage 的代码 */}
        {showAnalytics && <AnalyticsPage />}
      </Suspense>
    </div>
  );
}
```

**简历数据支撑**：
结合 `manualChunks` 和 `React.lazy`，项目实现了 **"首屏 bundle 减少 89% (669KB→71KB)"**。这说明原本巨大的代码块被成功拆分并延迟加载了。

## 4. Tree Shaking（摇树优化）是什么？

### 核心概念
**Tree Shaking** 是一个术语，通常用于描述移除 JavaScript 上下文中**死代码（Dead Code）**的过程。它依赖于 ES2015 模块系统（`import` 和 `export`）的静态结构特性。

### 形象比喻
想象你的应用程序是一棵树，你使用的第三方库（如 lodash 或 utils）是树上的叶子。Tree Shaking 就是用力摇晃这棵树，把那些**没有被用到的叶子（未引用的代码）**摇下来，只保留绿色的、活着的叶子（实际使用的代码）。

### 解决了什么问题？
*   **减小打包体积**：很多第三方库非常大，但你可能只用了其中一两个函数。如果没有 Tree Shaking，整个库都会被打包进去。
*   **提升加载速度**：体积越小，下载和解析越快。

### 在本项目中的应用
简历中提到 *"通过 Tree Shaking、Gzip 压缩优化资源加载"*。
*   **Vite/Rollup 的作用**：Vite 生产环境构建使用 Rollup，它天生支持 Tree Shaking。
*   **实战案例**：假设你引入了 `import { formatDate } from './utils'`，而 `utils.ts` 中还有 50 个其他函数。构建工具会分析出只有 `formatDate` 被使用了，最终打包产物中**只会包含 formatDate 的代码**，其他 50 个函数会被丢弃。

## 5. Prettier 是什么？

### 核心概念
**Prettier** 是一个**代码格式化工具（Code Formatter）**。它通过解析代码并使用自己的规则重新打印它（Reprinting），来强制执行一致的代码风格。

### 解决了什么问题？
*   **统一代码风格**：解决团队成员关于“使用单引号还是双引号”、“行尾是否加分号”、“缩进用2空格还是4空格”的争论。
*   **提升可读性**：自动处理长代码的换行，使代码整洁易读。
*   **节省时间**：开发者不再需要手动调整格式，保存文件时自动格式化。

### 在本项目中的应用
简历中提到 *"使用 ESLint + Prettier 建立代码规范"*。
*   **ESLint vs Prettier**：
    *   **ESLint** 主要负责**代码质量**（如：禁止使用未定义的变量、强制使用 `===`）。
    *   **Prettier** 主要负责**代码风格**（如：缩进、换行、引号）。
*   两者配合使用，确保项目代码既没有低级错误，又保持风格高度一致。

## 6. React Hooks 是什么？

### 核心概念
**React Hooks** 是 React 16.8 引入的新特性，允许你在**不编写 class 组件的情况下使用 state 以及其他的 React 特性**。

### 解决了什么问题？
1.  **复用状态逻辑**：在 Class 组件时代，复用逻辑通常需要高阶组件（HOC）或 Render Props，导致组件层级嵌套过深（Wrapper Hell）。Hooks 允许我们将逻辑提取为自定义 Hook（如 `useFetch`），在不同组件间轻松复用。
2.  **组件逻辑聚合**：Class 组件强制将相关逻辑拆分到 `componentDidMount`、`componentDidUpdate` 等生命周期中。Hooks（如 `useEffect`）允许将相关的副作用逻辑写在一起。
3.  **拥抱函数式编程**：让组件更轻量，避免了 `this` 指向的困扰。

### 在本项目中的应用
简历中提到 *"使用 React Hooks（useState、useEffect、useCallback、useMemo）实现状态管理"*。
*   **useState**：管理地图的灾害数据列表、Loading 状态。
*   **useEffect**：处理副作用，如组件加载时请求 API、设置定时器自动刷新。
*   **useCallback**：缓存事件处理函数（如点击地图标记），避免子组件不必要的重渲染。

## 7. useMemo 是什么？

### 核心概念
**useMemo** 是一个 React Hook，用于**缓存计算结果（Memoization）**。它接收一个“创建”函数和一个依赖项数组，只有当依赖项发生变化时，才会重新计算。

### 解决了什么问题？
*   **避免昂贵的重复计算**：如果一个计算过程非常耗时（如遍历 10000 条数据进行过滤、排序、聚合），我们不希望每次组件重渲染（Re-render）时都重新计算一遍。
*   **引用稳定性**：当对象或数组作为子组件的 props 时，使用 useMemo 可以保持引用不变，配合 `React.memo` 避免子组件不必要的重渲染。

### 在本项目中的应用
简历中提到 *"通过 useMemo 缓存计算结果优化大数据渲染性能"*。
**场景**：`ChartsPanel` 组件中的图表数据处理。
```typescript
// 示例：从原始灾害列表中计算时间线数据
const timelineData = React.useMemo(() => {
  // 这是一个昂贵的操作：遍历所有灾害数据，解析日期，排序，聚合
  return hazards
    .map(h => processDate(h))
    .sort((a, b) => a - b)
    .reduce((acc, curr) => { ... }, {});
}, [hazards]); // 只有当 hazards 数据源变化时，才重新计算
```
如果没有 `useMemo`，每次父组件更新（例如仅仅是 Loading 状态变化），这个昂贵的数组处理逻辑都会重新跑一遍，导致页面卡顿。

## 8. 去重处理（Deduplication）是什么？

### 核心概念
**去重处理**是指在数据处理过程中，识别并移除重复的数据项，确保数据的唯一性和准确性。

### 在本项目中的应用
简历中提到 *"实现数据格式标准化、去重处理"*。
**场景**：项目集成了 4 个数据源（DisasterAware, USGS, NASA, GDACS）。
*   **问题**：同一个大地震（例如“日本 7.0 级地震”）可能同时出现在 USGS 和 DisasterAware 的 API 返回结果中。如果直接渲染，地图上会出现两个重叠的标记，列表中也会有两条重复记录，给用户造成困扰。
*   **解决方案**：
    1.  **标准化**：首先将不同来源的数据统一转换为标准的 `Hazard` 接口。
    2.  **唯一标识**：确定唯一键（如：`经纬度 + 时间范围` 或 `官方事件ID`）。
    3.  **过滤逻辑**：在合并数据时，检查该事件是否已存在。如果存在，可能保留信息更全的那一条，或者合并两者的信息。

## 9. 错误降级（Error Fallback / Graceful Degradation）是什么？

### 核心概念
**错误降级**是一种容错设计策略。当系统中的某个部分发生故障时，系统不会完全崩溃，而是提供一个**简化的、功能有限的替代方案**，或者友好的错误提示，保证核心功能依然可用。

### 在本项目中的应用
简历中提到 *"错误降级（DisasterAware失败自动切换其他数据源）"* 和 *"ErrorBoundary组件"*。

**两个层面的降级：**
1.  **数据源层面（API 降级）**：
    *   **理想情况**：优先使用 DisasterAware API（数据最全）。
    *   **故障场景**：如果 DisasterAware API 返回 500 错误或超时。
    *   **降级策略**：系统自动捕获错误，并立即发起对 USGS 或 GDACS 的请求。虽然可能缺少某些详细字段，但用户依然能在地图上看到灾害点，保证了“监控”这一核心功能的可用性。
    *   *代码体现：`Promise.any` 或 `try-catch` 链式调用。*

2.  **UI 层面（组件降级）**：
    *   **场景**：某个图表组件因为数据格式异常抛出了 JS 错误。
    *   **降级策略**：使用 `ErrorBoundary` 包裹组件。当图表崩溃时，不会导致整个页面白屏，而是仅在图表区域显示“图表加载失败，请刷新重试”，其他区域（如地图、列表）依然可以正常操作。

## 10. 点击钻取（Click Drill-down）是什么？

### 核心概念
**点击钻取**是一种交互式数据分析技术。用户通过点击图表中的某个数据元素（如饼图的一个扇区、柱状图的一根柱子），触发数据的**层级下探**，查看该元素背后更详细的数据子集。

### 在本项目中的应用
简历中提到 *"支持点击钻取查看详细数据"*。
**场景**：`ChartsPanel` 组件。
*   **交互流程**：
    1.  用户看到“灾害类型分布”饼图，发现“洪水（Flood）”占比很大。
    2.  用户**点击**饼图中的“洪水”扇区。
    3.  **响应**：系统弹出一个模态框（Modal）或更新下方的列表，专门展示所有“洪水”类型的具体事件列表（时间、地点、严重程度）。
*   **技术实现**：
    *   Recharts 的 `onClick` 事件回调。
    *   React State 存储当前的 `drilldownFilter`（钻取过滤条件）。
    *   数据过滤逻辑：`hazards.filter(h => h.type === 'Flood')`。

## 11. （23种统计算法 + 5个预测模型）实现高级分析是什么？

### 核心概念
这指的是**后端数据处理能力**与前端可视化的结合。前端不仅仅是展示原始数据，还展示经过复杂计算后的**洞察（Insights）**。

### 在本项目中的应用
简历中提到 *"集成 Python FastAPI 微服务...实现高级分析"*。
*   **架构**：前端（React）收集到灾害数据 -> 发送给 Python 后端 -> 后端运行算法 -> 返回分析结果 -> 前端展示。
*   **具体内容**：
    *   **23种统计算法**：可能包括平均值、中位数、标准差、方差分析、趋势斜率计算等，用于生成 `StatisticsCard` 中的数据。
    *   **5个预测模型**：可能包括线性回归（预测灾害增长趋势）、DBSCAN（地理聚类分析）、时间序列预测等。
*   **前端价值**：前端工程师负责设计 API 接口格式，处理异步请求，并将复杂的数学结果转化为直观的图表或风险评分（Risk Score）。

## 12. Mapbox GL JS 3.15 是什么？

### 核心概念
**Mapbox GL JS** 是一个用于在 Web 上渲染交互式地图的 JavaScript 库。它利用 **WebGL** 技术，可以直接在浏览器中渲染矢量瓦片（Vector Tiles），支持流畅的缩放、旋转和 3D 效果。

### 解决了什么问题？
*   **高性能渲染**：相比传统的栅格地图（图片拼接），WebGL 可以利用 GPU 加速，轻松渲染成千上万个动态标记点（Markers）而不卡顿。
*   **3D 地球视图**：v3.x 版本引入了 `projection: 'globe'`，可以原生展示 3D 地球形态，而不是平面的墨卡托投影，非常适合全球灾害监控场景。

### 在本项目中的应用
简历中提到 *"基于 Mapbox GL JS 3.15 开发交互式 3D 地球视图"*。
*   **核心功能**：加载地图底图、渲染灾害标记点、处理地图交互（缩放、旋转）、展示 Popup 弹窗。

## 13. GeoJSON 格式是什么？

### 核心概念
**GeoJSON** 是一种基于 JSON 的格式，用于编码各种地理数据结构。它是地理信息系统（GIS）和 Web 地图开发中的**标准数据交换格式**。

### 结构示例
```json
{
  "type": "Feature",
  "geometry": {
    "type": "Point",
    "coordinates": [125.6, 10.1] // [经度, 纬度]
  },
  "properties": {
    "name": "Dinagat Islands"
  }
}
```

### 在本项目中的应用
简历中提到 *"使用 GeoJSON 格式渲染 200-400 条实时灾害标记"*。
*   **数据转换**：后端 API 返回的数据格式可能各不相同。前端需要编写适配器（Adapter），将它们统一转换为 GeoJSON `FeatureCollection` 格式。
*   **地图渲染**：Mapbox GL JS 原生支持 GeoJSON 数据源（`Source`）和图层（`Layer`），可以直接将 GeoJSON 数据渲染为地图上的圆点、热力图或 3D 柱状图。

## 14. 全球范围灾害实时追踪怎么实现？

### 核心概念
这是一个**系统设计**问题，涉及数据采集、处理、分发和展示的全链路。

### 在本项目中的实现逻辑
1.  **多源采集**：后端或前端定时从 USGS（地震）、NASA（火灾）、GDACS（综合）等接口拉取数据。
2.  **数据融合**：将不同来源的数据清洗、去重、标准化为统一格式。
3.  **状态更新**：前端 React 组件接收到新数据，更新 State。
4.  **地图渲染**：Mapbox 根据新的 State 数据，实时更新地图上的标记点位置和颜色。
5.  **轮询机制**：通过 `setInterval` 每隔 5 分钟自动执行一次上述流程，确保数据“实时”。

## 15. Promise.allSettled 并发请求是什么？

### 核心概念
**Promise.allSettled()** 是 ES2020 引入的方法。它接收一个 Promise 数组，**并发执行**所有 Promise。
*   **与 Promise.all 的区别**：`Promise.all` 只要有一个失败，整个就直接 Reject（挂掉）。`Promise.allSettled` 会等待**所有** Promise 完成，无论成功还是失败，并返回每个 Promise 的结果状态（fulfilled 或 rejected）。

### 解决了什么问题？
**容错性（Resilience）**。

### 在本项目中的应用
简历中提到 *"设计 Promise.allSettled 并发请求 + 错误降级机制"*。
**场景**：同时请求 4 个数据源。
*   **问题**：如果使用 `Promise.all`，只要 NASA 的 API 挂了，整个页面的数据加载就会失败，用户连 USGS 的地震数据也看不了。
*   **解决方案**：使用 `Promise.allSettled`。
    *   如果 NASA 挂了，USGS 成功了。
    *   代码会遍历结果数组，收集所有 `status === 'fulfilled'` 的数据进行展示。
    *   对于 `status === 'rejected'` 的部分，记录错误日志或显示局部警告，但不影响整体页面运行。

## 16. 自动数据刷新、实时通知是什么？

### 核心概念
提升用户体验的机制，让用户不需要手动刷新页面就能看到最新信息。

### 在本项目中的应用
简历中提到 *"实现自动数据刷新、实时通知机制"*。

**1. 自动刷新 (Auto Refresh)**
*   **实现**：使用 `useEffect` 和 `setInterval`。
    ```typescript
    useEffect(() => {
      const timer = setInterval(() => {
        fetchData(); // 每5分钟重新请求一次 API
      }, 5 * 60 * 1000);
      return () => clearInterval(timer); // 组件卸载时清除定时器
    }, []);
    ```

**2. 实时通知 (Real-time Notification)**
*   **逻辑**：
    1.  记录上一次获取的数据条数（`prevCount`）。
    2.  当新数据回来时，计算新条数（`newCount`）。
    3.  如果 `newCount > prevCount`，说明有新灾害发生。
    4.  触发通知组件（如 `NotificationCenter` 或 Toast 弹窗），显示“检测到 3 条新灾害记录”。
*   **价值**：让监控人员可以挂着页面后台运行，一旦有新情况能立刻得到反馈。
